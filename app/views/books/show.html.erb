<% if @book.errors.any? %>
<%= @book.errors.count %>errors prohibited this obj from being saved:
<% @book.errors.full_messages.each do |message| %>
<%= message %>
<% end %>
<% end %>
<h1>Book detil</h1>
<p><%= link_to user_path(@user) do %>
  <%=attachment_image_tag @user, :profile_image, :fill, 20, 20, fot: "jpg", fallback: "no_image.jpg" %>
  <%= @user.name %></p>
  <% end %>
<p><%= @book.title %></p>
<p><%= @book.body %></p>
<% if @book.user_id == current_user.id %>
<p><%= link_to "edit", edit_book_path(@book) %></P>
<p><%= link_to "destroy", book_path(@book), method: :delete, "date-confirm" => "本当に削除しますか" %></p>
<% end %>
<h1>User info</h1>
 <%=attachment_image_tag @user, :profile_image, :fill, 60, 60, fot: "jpg", fallback: "no_image.jpg" %>
<p>name</p>
<%= @user.name %>
<p>introduction</p>
<%= @user.introduction %>
<h1>New Book</h1>
<%= render "books/newbook", book: @newbook %>

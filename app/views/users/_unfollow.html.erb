<%= form_with(model: current_user.active_relationships.find(params[:followed_id]),
            html: { method: :delete }, local: true) do |f| %>
  <%= f.submit "フォロー外す", class: "btn btn-secondary" %>
<% end %>